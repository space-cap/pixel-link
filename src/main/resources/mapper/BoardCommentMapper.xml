<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pixellink.mapper.BoardCommentMapper">

  <select id="findByArticleId" resultType="com.pixellink.model.BoardComment">
    SELECT * FROM board_comments 
    WHERE article_id = #{articleId}
    ORDER BY created_at ASC
  </select>

  <insert id="insert">
    INSERT INTO board_comments (id, article_id, author_id, author_name, content, is_admin_reply, created_at)
    VALUES (
      #{id}, 
      #{articleId}, 
      #{authorId, jdbcType=VARCHAR}, 
      #{authorName}, 
      #{content}, 
      #{isAdminReply}, 
      #{createdAt}
    )
  </insert>

  <delete id="delete">
    DELETE FROM board_comments WHERE id = #{id}
  </delete>

</mapper>
